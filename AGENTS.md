# AGENTS.md — AI エージェント行動規則

本ファイルは、PR レビュー時に AI エージェント（Codex 等）が従うべき行動規則とプロジェクト情報を定めるものである。

---

## 1. プロジェクト概要

| 項目       | 内容                                   |
| ---------- | -------------------------------------- |
| アプリ名   | 最新型家計簿！「レスマネ」             |
| チーム名   | 節約志向になりたい                     |
| 種別       | 学生向け AI 連動家計簿 Web アプリ      |
| リポジトリ | space-musicacct/resmane                |

---

## 2. 技術構成

| 層               | 技術                    | バージョン  |
| ---------------- | ----------------------- | ----------- |
| フロントエンド   | React + TypeScript      | React 18 / TS 5 |
| スタイリング     | Tailwind CSS            | 3 系        |
| ビルド           | Vite                    | 8 系        |
| バックエンド     | PHP + Laravel           | PHP 8.5 / Laravel 13 |
| AI worker        | Python + schedule       | Python 3.11 |
| データベース     | MySQL                   | 8.4         |
| インフラ         | Docker Compose + Nginx  | -           |

### アーキテクチャ上の重要事項

- フロントエンド（React SPA）とバックエンド（Laravel API）は**完全に分離**する。React は Laravel の `resources/` 配下に置かない
- Nginx がパスで振り分け：静的ファイル → React、`/api` → Laravel
- AI 処理は Python worker が非同期で担う。Laravel 本体から外部 AI API を直接呼ばない

詳細：`docs/技術構成/技術構成書.md`

---

## 3. コーディング規約（要約）

| 言語       | 規約   | インデント | 整形ツール       | 命名                                   |
| ---------- | ------ | ---------- | ---------------- | -------------------------------------- |
| PHP        | PSR-12 | 4 spaces   | Laravel Pint     | クラス: PascalCase / メソッド・変数: camelCase |
| TypeScript | -      | 2 spaces   | ESLint + Prettier | コンポーネント: PascalCase / 変数・関数: camelCase |
| Python     | PEP 8  | 4 spaces   | Black + Ruff     | 関数・変数: snake_case / クラス: PascalCase |
| DB         | -      | -          | -                | テーブル: snake_case 複数形 / カラム: snake_case |

詳細：`docs/開発ルール/コーディング規約.md`

---

## 4. Git 運用

- ブランチ戦略：`main` ← `develop` ← `feature/{担当者}/{トピック}`
- すべてのマージは PR 経由（直接 push 禁止）
- force push 禁止
- コミットメッセージ：日本語可。1行目に変更内容の要約

---

## 5. 行動規則

### 禁止事項

以下の行動を**厳禁**とする。

1. **コミット・PR の無断作成** — 「〜を作成してください」「〜をコミットしてください」等の明示的指示がない限り、コミットの作成や PR の作成を行わない
2. **虚偽の行動報告** — 実際に実行していないアクション（コミット作成・PR 作成・ファイル変更等）を「行った」「作成済み」と報告しない
3. **スコープの拡大** — レビュー対象の diff に含まれない変更を提案しない。ドキュメントのみの PR に対して、設定ファイルの追加やコード変更を提案しない
4. **主導権の逸脱** — チームメンバーから明確に委任されていない作業を独断で行わない。判断に迷う場合は確認を求める
5. **diff 外ファイルへの指摘** — PR で変更されていないファイルに対する指摘・改善提案を行わない

### 許可される行動

1. PR の diff に対するレビューコメント（指摘・改善案・質問）
2. 規約違反の指摘（根拠となる規約セクションを引用すること）
3. セキュリティ上の懸念の指摘
4. 不明点に対する質問・確認

---

## 6. レビュー時の着眼点

1. **命名規則** — コーディング規約（3 章）に沿った命名か
2. **アーキテクチャ分離** — フロント / バック / worker の境界が守られているか
3. **型安全性** — TypeScript の型が適切に使われているか
4. **セキュリティ** — SQLインジェクション、XSS、CSRF、入力値検証、APIキー漏洩
5. **diff 集中** — 上記すべて、PR の diff 範囲内でのみ確認する

---

## 7. 出力フォーマット

- レビューコメントは**日本語**で記述する
- 規約違反を指摘する際は、根拠となるドキュメントのセクションを引用する
  - 例：「コーディング規約 3.2 命名規則に反しています」
- 重要度ラベルを付与する：
  - `[必須修正]` — マージ前に修正すべき問題
  - `[推奨]` — 修正が望ましいが必須ではない
  - `[質問]` — 意図の確認
